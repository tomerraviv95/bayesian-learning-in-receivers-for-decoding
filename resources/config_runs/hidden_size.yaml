##  DISCLAIMERS ON INPUT SIZES
# Note that due to coding length, both the block length and pilots size have to be divisible by the message bits.
# Also, for the QPSK modulation one has to have an even number of pre-modulated bits thus
# x = block_length * (code_bits/message_bits) have to be even.
# For the same reason, in 8-PSK this quantity x has to divisible by 4.

# general
seed: 10 # run seed

# MIMO protocol
block_length: 15360 # coherence block time, total size of pilot + data. values: int.
pilots_length: 3840 # pilot bits
blocks_num: 20 # number of validation frames. values: int.
n_user: 4 # number of transmitting devices
n_ant: 4 # number of received signals

# datasets
channel_model: 'Synthetic' # ['Synthetic','Cost2100']
linear: True # datasets linearity. Boolean value.
fading_in_channel: True # If the datasets is fading/non-fading. values: [True, False]
snr: 14 # validation snr value. values: float.
modulation_type: 'EightPSK' # ['BPSK','QPSK','EightPSK']
detector_type: 'seq_model' # ['seq_model','cmodel_based_bayesian','bayesian','end_to_end','model_based_bayesian']

# coding hyperparams
code_type: 'POLAR' # ['BCH','POLAR']
code_bits: 128
message_bits: 64
decoder_type: 'wbp' # ['bp','wbp','modular_bayesian_wbp','bayesian_wbp']

# training hyperparameters
loss_type: 'CrossEntropy' # Loss type. values: 'BCE','CrossEntropy','MSE'.
optimizer_type: 'Adam' # Optimizer type. values: 'Adam','RMSprop','SGD'.
hidden_base_size: 16 # size of the hidden layer in DeepSIC